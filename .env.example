# No private API keys are required for this project.

# The contact form uses a Web3Forms public access key directly in:
# src/components/ContactPage.jsx
# This key is safe for client-side code and public GitHub repositories.

# If you later add private backend secrets, put them in a local .env file
# and keep .env listed in .gitignore.
